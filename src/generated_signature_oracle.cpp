// GENERATED by tools/generate_signature_oracles.py - do not edit.
//
// Each function below is executed in its ORIGINAL form, at its canonical
// address inside the hybrid process, and compared against the recovered
// implementation reached through the same address with the redirect
// restored. Return value AND the globals each side produces are compared;
// these functions return void as often as not and do their work in .data.
//
// The markers below are the input to `unproven_recovered` in
// docs/recovery/summary.json; tools/export_proven_functions.py decides
// what counts.
//
// PROOFS NOT CLAIMED: see the note above emit() in
// tools/generate_signature_oracles.py. These oracles run and report;
// they do not yet lower unproven_recovered.

#include "stdafx.h"
#include "generated_signature_oracle.h"

#include "runtime_oracle.h"

#include <cstdio>
#include <cstdint>
#include <cstring>
#include <vector>

namespace {

// .data and .bss of the original image. The recovered bodies write here
// and so do the originals; comparing it is what makes a void-returning
// function testable at all.
constexpr uintptr_t GlobalsBegin = 0x00682000U;
constexpr uintptr_t GlobalsEnd   = 0x009C21F8U;
constexpr size_t GlobalsSize = GlobalsEnd - GlobalsBegin;

void snapshot(std::vector<uint8_t> &into) {
    into.resize(GlobalsSize);
    std::memcpy(into.data(), reinterpret_cast<const void *>(GlobalsBegin),
                GlobalsSize);
}

void restore(const std::vector<uint8_t> &from) {
    std::memcpy(reinterpret_cast<void *>(GlobalsBegin), from.data(),
                GlobalsSize);
}

// Where the two images first disagree, for a message that names an address
// rather than saying only that something differed.
bool same_globals(const std::vector<uint8_t> &a,
                  const std::vector<uint8_t> &b, uintptr_t *where) {
    for (size_t index = 0; index < GlobalsSize; ++index) {
        if (a[index] != b[index]) {
            *where = GlobalsBegin + index;
            return false;
        }
    }
    return true;
}

}  // namespace

// ?energy_limit@@YAHH@Z
// recovered in src/game_recovery.cpp:42
static bool verify_energy_limit_00445440() {
    typedef int (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x00445440U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x00445440U)) {
            std::printf("  ?energy_limit@@YAHH@Z: cannot suspend redirect\n");
            return false;
        }
        const int original_result = target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x00445440U)) {
            std::printf("  ?energy_limit@@YAHH@Z: cannot resume redirect\n");
            return false;
        }
        const int recovered_result = target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        if (original_result != recovered_result) {
            std::printf("  ?energy_limit@@YAHH@Z: return value differs\n");
            passed = false;
        }
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?energy_limit@@YAHH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?passover_callback@@YAXXZ
// recovered in src/guarded_teardowns.cpp:104
static bool verify_passover_callback_004456a0() {
    typedef void (__cdecl *Callable)();
    Callable target = reinterpret_cast<Callable>(0x004456A0U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    {
        snapshot(before);
        if (!suspend_redirect_at(0x004456A0U)) {
            std::printf("  ?passover_callback@@YAXXZ: cannot suspend redirect\n");
            return false;
        }
        target();
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x004456A0U)) {
            std::printf("  ?passover_callback@@YAXXZ: cannot resume redirect\n");
            return false;
        }
        target();
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?passover_callback@@YAXXZ: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?help_tech@@YAXH@Z
// recovered in src/datalink.cpp:264
static bool verify_help_tech_0044c880() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x0044C880U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0044C880U)) {
            std::printf("  ?help_tech@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0044C880U)) {
            std::printf("  ?help_tech@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?help_tech@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?help_weapon@@YAXH@Z
// recovered in src/datalink.cpp:274
static bool verify_help_weapon_0044c910() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x0044C910U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0044C910U)) {
            std::printf("  ?help_weapon@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0044C910U)) {
            std::printf("  ?help_weapon@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?help_weapon@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?help_armor@@YAXH@Z
// recovered in src/datalink.cpp:284
static bool verify_help_armor_0044c940() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x0044C940U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0044C940U)) {
            std::printf("  ?help_armor@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0044C940U)) {
            std::printf("  ?help_armor@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?help_armor@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?help_chassis@@YAXH@Z
// recovered in src/datalink.cpp:294
static bool verify_help_chassis_0044c980() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x0044C980U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0044C980U)) {
            std::printf("  ?help_chassis@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0044C980U)) {
            std::printf("  ?help_chassis@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?help_chassis@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?help_facility@@YAXH@Z
// recovered in src/datalink.cpp:304
static bool verify_help_facility_0044c9b0() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x0044C9B0U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0044C9B0U)) {
            std::printf("  ?help_facility@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0044C9B0U)) {
            std::printf("  ?help_facility@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?help_facility@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?help_abil@@YAXH@Z
// recovered in src/datalink.cpp:314
static bool verify_help_abil_0044ca10() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x0044CA10U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0044CA10U)) {
            std::printf("  ?help_abil@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0044CA10U)) {
            std::printf("  ?help_abil@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?help_abil@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?help_social@@YAXH@Z
// recovered in src/datalink.cpp:324
static bool verify_help_social_0044ca40() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x0044CA40U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0044CA40U)) {
            std::printf("  ?help_social@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0044CA40U)) {
            std::printf("  ?help_social@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?help_social@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?help_faction@@YAXH@Z
// recovered in src/datalink.cpp:334
static bool verify_help_faction_0044ca70() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x0044CA70U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0044CA70U)) {
            std::printf("  ?help_faction@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0044CA70U)) {
            std::printf("  ?help_faction@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?help_faction@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?help_veh@@YAXH@Z
// recovered in src/datalink.cpp:344
static bool verify_help_veh_0044cad0() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x0044CAD0U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0044CAD0U)) {
            std::printf("  ?help_veh@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0044CAD0U)) {
            std::printf("  ?help_veh@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?help_veh@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?help_topic@@YAXIH@Z
// recovered in src/datalink.cpp:356
static bool verify_help_topic_0044cb60() {
    typedef void (__cdecl *Callable)(unsigned int, int);
    Callable target = reinterpret_cast<Callable>(0x0044CB60U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][2] = {
        {0, 1},
        {1, -1},
        {-1, 2},
        {2, 7},
        {7, 2147483647},
        {2147483647, -2147483648},
        {-2147483648, 1431655765},
        {1431655765, 0},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0044CB60U)) {
            std::printf("  ?help_topic@@YAXIH@Z: cannot suspend redirect\n");
            return false;
        }
        target((unsigned int)argv[0], (int)argv[1]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0044CB60U)) {
            std::printf("  ?help_topic@@YAXIH@Z: cannot resume redirect\n");
            return false;
        }
        target((unsigned int)argv[0], (int)argv[1]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?help_topic@@YAXIH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?load_deswin_sprites@@YAXXZ
// recovered in src/leaf_recoveries.cpp:1304
static bool verify_load_deswin_sprites_00455e50() {
    typedef void (__cdecl *Callable)();
    Callable target = reinterpret_cast<Callable>(0x00455E50U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    {
        snapshot(before);
        if (!suspend_redirect_at(0x00455E50U)) {
            std::printf("  ?load_deswin_sprites@@YAXXZ: cannot suspend redirect\n");
            return false;
        }
        target();
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x00455E50U)) {
            std::printf("  ?load_deswin_sprites@@YAXXZ: cannot resume redirect\n");
            return false;
        }
        target();
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?load_deswin_sprites@@YAXXZ: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?draw_tile@@YAXHHH@Z
// recovered in src/mapwin.cpp:202
static bool verify_draw_tile_0046af40() {
    typedef void (__cdecl *Callable)(int, int, int);
    Callable target = reinterpret_cast<Callable>(0x0046AF40U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][3] = {
        {0, 1, -1},
        {1, -1, 2},
        {-1, 2, 7},
        {2, 7, 2147483647},
        {7, 2147483647, -2147483648},
        {2147483647, -2147483648, 1431655765},
        {-2147483648, 1431655765, 0},
        {1431655765, 0, 1},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0046AF40U)) {
            std::printf("  ?draw_tile@@YAXHHH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0], (int)argv[1], (int)argv[2]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0046AF40U)) {
            std::printf("  ?draw_tile@@YAXHHH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0], (int)argv[1], (int)argv[2]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?draw_tile@@YAXHHH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?draw_tiles@@YAXHHH@Z
// recovered in src/mapwin.cpp:249
static bool verify_draw_tiles_0046b140() {
    typedef void (__cdecl *Callable)(int, int, int);
    Callable target = reinterpret_cast<Callable>(0x0046B140U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][3] = {
        {0, 1, -1},
        {1, -1, 2},
        {-1, 2, 7},
        {2, 7, 2147483647},
        {7, 2147483647, -2147483648},
        {2147483647, -2147483648, 1431655765},
        {-2147483648, 1431655765, 0},
        {1431655765, 0, 1},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0046B140U)) {
            std::printf("  ?draw_tiles@@YAXHHH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0], (int)argv[1], (int)argv[2]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0046B140U)) {
            std::printf("  ?draw_tiles@@YAXHHH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0], (int)argv[1], (int)argv[2]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?draw_tiles@@YAXHHH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?base_at@@YAHHH@Z
// recovered in src/base.cpp:98
static bool verify_base_at_004e3a50() {
    typedef int (__cdecl *Callable)(int, int);
    Callable target = reinterpret_cast<Callable>(0x004E3A50U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][2] = {
        {0, 1},
        {1, -1},
        {-1, 2},
        {2, 7},
        {7, 2147483647},
        {2147483647, -2147483648},
        {-2147483648, 1431655765},
        {1431655765, 0},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x004E3A50U)) {
            std::printf("  ?base_at@@YAHHH@Z: cannot suspend redirect\n");
            return false;
        }
        const int original_result = target((int)argv[0], (int)argv[1]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x004E3A50U)) {
            std::printf("  ?base_at@@YAHHH@Z: cannot resume redirect\n");
            return false;
        }
        const int recovered_result = target((int)argv[0], (int)argv[1]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        if (original_result != recovered_result) {
            std::printf("  ?base_at@@YAHHH@Z: return value differs\n");
            passed = false;
        }
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?base_at@@YAHHH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?mandate_color@@YAHH@Z
// recovered in src/map_color.cpp:31
static bool verify_mandate_color_0050e820() {
    typedef int (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x0050E820U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0050E820U)) {
            std::printf("  ?mandate_color@@YAHH@Z: cannot suspend redirect\n");
            return false;
        }
        const int original_result = target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0050E820U)) {
            std::printf("  ?mandate_color@@YAHH@Z: cannot resume redirect\n");
            return false;
        }
        const int recovered_result = target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        if (original_result != recovered_result) {
            std::printf("  ?mandate_color@@YAHH@Z: return value differs\n");
            passed = false;
        }
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?mandate_color@@YAHH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?not_my_turn@@YAHXZ
// recovered in src/game.cpp:53
static bool verify_not_my_turn_0052dc70() {
    typedef int (__cdecl *Callable)();
    Callable target = reinterpret_cast<Callable>(0x0052DC70U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    {
        snapshot(before);
        if (!suspend_redirect_at(0x0052DC70U)) {
            std::printf("  ?not_my_turn@@YAHXZ: cannot suspend redirect\n");
            return false;
        }
        const int original_result = target();
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0052DC70U)) {
            std::printf("  ?not_my_turn@@YAHXZ: cannot resume redirect\n");
            return false;
        }
        const int recovered_result = target();
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        if (original_result != recovered_result) {
            std::printf("  ?not_my_turn@@YAHXZ: return value differs\n");
            passed = false;
        }
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?not_my_turn@@YAHXZ: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?spying@@YAHH@Z
// recovered in src/spying_recovery.cpp:23
static bool verify_spying_0055bc00() {
    typedef int (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x0055BC00U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x0055BC00U)) {
            std::printf("  ?spying@@YAHH@Z: cannot suspend redirect\n");
            return false;
        }
        const int original_result = target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0055BC00U)) {
            std::printf("  ?spying@@YAHH@Z: cannot resume redirect\n");
            return false;
        }
        const int recovered_result = target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        if (original_result != recovered_result) {
            std::printf("  ?spying@@YAHH@Z: return value differs\n");
            passed = false;
        }
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?spying@@YAHH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?find_font@@YAHHH@Z
// recovered in src/font_recovery.cpp:59
static bool verify_find_font_005882f0() {
    typedef int (__cdecl *Callable)(int, int);
    Callable target = reinterpret_cast<Callable>(0x005882F0U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][2] = {
        {0, 1},
        {1, -1},
        {-1, 2},
        {2, 7},
        {7, 2147483647},
        {2147483647, -2147483648},
        {-2147483648, 1431655765},
        {1431655765, 0},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x005882F0U)) {
            std::printf("  ?find_font@@YAHHH@Z: cannot suspend redirect\n");
            return false;
        }
        const int original_result = target((int)argv[0], (int)argv[1]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x005882F0U)) {
            std::printf("  ?find_font@@YAHHH@Z: cannot resume redirect\n");
            return false;
        }
        const int recovered_result = target((int)argv[0], (int)argv[1]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        if (original_result != recovered_result) {
            std::printf("  ?find_font@@YAHHH@Z: return value differs\n");
            passed = false;
        }
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?find_font@@YAHHH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?desktop_update@@YAXXZ
// recovered in src/maininterface.cpp:42
static bool verify_desktop_update_0058ee50() {
    typedef void (__cdecl *Callable)();
    Callable target = reinterpret_cast<Callable>(0x0058EE50U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    {
        snapshot(before);
        if (!suspend_redirect_at(0x0058EE50U)) {
            std::printf("  ?desktop_update@@YAXXZ: cannot suspend redirect\n");
            return false;
        }
        target();
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x0058EE50U)) {
            std::printf("  ?desktop_update@@YAXXZ: cannot resume redirect\n");
            return false;
        }
        target();
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?desktop_update@@YAXXZ: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_veh@@YAXH@Z
// recovered in src/netdaemon.cpp:65
static bool verify_synch_veh_00593220() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x00593220U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x00593220U)) {
            std::printf("  ?synch_veh@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x00593220U)) {
            std::printf("  ?synch_veh@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_veh@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_base@@YAXH@Z
// recovered in src/netdaemon.cpp:75
static bool verify_synch_base_00593250() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x00593250U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x00593250U)) {
            std::printf("  ?synch_base@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x00593250U)) {
            std::printf("  ?synch_base@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_base@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_energy@@YAXH@Z
// recovered in src/netdaemon.cpp:85
static bool verify_synch_energy_00593280() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x00593280U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x00593280U)) {
            std::printf("  ?synch_energy@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x00593280U)) {
            std::printf("  ?synch_energy@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_energy@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_researching@@YAXH@Z
// recovered in src/netdaemon.cpp:95
static bool verify_synch_researching_005932b0() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x005932B0U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x005932B0U)) {
            std::printf("  ?synch_researching@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x005932B0U)) {
            std::printf("  ?synch_researching@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_researching@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_leader@@YAXH@Z
// recovered in src/netdaemon.cpp:105
static bool verify_synch_leader_005932e0() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x005932E0U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x005932E0U)) {
            std::printf("  ?synch_leader@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x005932E0U)) {
            std::printf("  ?synch_leader@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_leader@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_ai@@YAXH@Z
// recovered in src/netdaemon.cpp:115
static bool verify_synch_ai_00593310() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x00593310U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x00593310U)) {
            std::printf("  ?synch_ai@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x00593310U)) {
            std::printf("  ?synch_ai@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_ai@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_research@@YAXH@Z
// recovered in src/netdaemon.cpp:125
static bool verify_synch_research_00593340() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x00593340U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x00593340U)) {
            std::printf("  ?synch_research@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x00593340U)) {
            std::printf("  ?synch_research@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_research@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_alloc@@YAXH@Z
// recovered in src/netdaemon.cpp:136
static bool verify_synch_alloc_00593390() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x00593390U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x00593390U)) {
            std::printf("  ?synch_alloc@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x00593390U)) {
            std::printf("  ?synch_alloc@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_alloc@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_soc@@YAXH@Z
// recovered in src/netdaemon.cpp:146
static bool verify_synch_soc_005933c0() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x005933C0U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x005933C0U)) {
            std::printf("  ?synch_soc@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x005933C0U)) {
            std::printf("  ?synch_soc@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_soc@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_proto@@YAXH@Z
// recovered in src/netdaemon.cpp:156
static bool verify_synch_proto_005933f0() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x005933F0U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x005933F0U)) {
            std::printf("  ?synch_proto@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x005933F0U)) {
            std::printf("  ?synch_proto@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_proto@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_obs@@YAXH@Z
// recovered in src/netdaemon.cpp:166
static bool verify_synch_obs_00593420() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x00593420U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x00593420U)) {
            std::printf("  ?synch_obs@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x00593420U)) {
            std::printf("  ?synch_obs@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_obs@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_diplo@@YAXHH@Z
// recovered in src/netdaemon.cpp:178
static bool verify_synch_diplo_00593450() {
    typedef void (__cdecl *Callable)(int, int);
    Callable target = reinterpret_cast<Callable>(0x00593450U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][2] = {
        {0, 1},
        {1, -1},
        {-1, 2},
        {2, 7},
        {7, 2147483647},
        {2147483647, -2147483648},
        {-2147483648, 1431655765},
        {1431655765, 0},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x00593450U)) {
            std::printf("  ?synch_diplo@@YAXHH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0], (int)argv[1]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x00593450U)) {
            std::printf("  ?synch_diplo@@YAXHH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0], (int)argv[1]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_diplo@@YAXHH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_template@@YAXH@Z
// recovered in src/netdaemon.cpp:188
static bool verify_synch_template_005934b0() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x005934B0U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x005934B0U)) {
            std::printf("  ?synch_template@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x005934B0U)) {
            std::printf("  ?synch_template@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_template@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?synch_radius@@YAXH@Z
// recovered in src/netdaemon.cpp:198
static bool verify_synch_radius_005934e0() {
    typedef void (__cdecl *Callable)(int);
    Callable target = reinterpret_cast<Callable>(0x005934E0U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    static const long long cases[][1] = {
        {0},
        {1},
        {-1},
        {2},
        {7},
        {2147483647},
        {-2147483648},
        {1431655765},
    };
    for (const auto &argv : cases) {
        snapshot(before);
        if (!suspend_redirect_at(0x005934E0U)) {
            std::printf("  ?synch_radius@@YAXH@Z: cannot suspend redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x005934E0U)) {
            std::printf("  ?synch_radius@@YAXH@Z: cannot resume redirect\n");
            return false;
        }
        target((int)argv[0]);
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?synch_radius@@YAXH@Z: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

// ?do_sound@@YAXXZ
// recovered in src/autosound.cpp:144
static bool verify_do_sound_005fd2b0() {
    typedef void (__cdecl *Callable)();
    Callable target = reinterpret_cast<Callable>(0x005FD2B0U);
    std::vector<uint8_t> before, after_original, after_recovered;
    bool passed = true;
    {
        snapshot(before);
        if (!suspend_redirect_at(0x005FD2B0U)) {
            std::printf("  ?do_sound@@YAXXZ: cannot suspend redirect\n");
            return false;
        }
        target();
        snapshot(after_original);
        restore(before);
        if (!resume_redirect_at(0x005FD2B0U)) {
            std::printf("  ?do_sound@@YAXXZ: cannot resume redirect\n");
            return false;
        }
        target();
        snapshot(after_recovered);
        restore(before);   // leave the process as it was found
        uintptr_t where = 0;
        if (!same_globals(after_original, after_recovered, &where)) {
            std::printf("  ?do_sound@@YAXXZ: globals differ, first at %p\n",
                        reinterpret_cast<void *>(where));
            passed = false;
        }
    }
    return passed;
}

bool run_generated_signature_oracles() {
    bool passed = true;
    passed &= verify_energy_limit_00445440();
    passed &= verify_passover_callback_004456a0();
    passed &= verify_help_tech_0044c880();
    passed &= verify_help_weapon_0044c910();
    passed &= verify_help_armor_0044c940();
    passed &= verify_help_chassis_0044c980();
    passed &= verify_help_facility_0044c9b0();
    passed &= verify_help_abil_0044ca10();
    passed &= verify_help_social_0044ca40();
    passed &= verify_help_faction_0044ca70();
    passed &= verify_help_veh_0044cad0();
    passed &= verify_help_topic_0044cb60();
    passed &= verify_load_deswin_sprites_00455e50();
    passed &= verify_draw_tile_0046af40();
    passed &= verify_draw_tiles_0046b140();
    passed &= verify_base_at_004e3a50();
    passed &= verify_mandate_color_0050e820();
    passed &= verify_not_my_turn_0052dc70();
    passed &= verify_spying_0055bc00();
    passed &= verify_find_font_005882f0();
    passed &= verify_desktop_update_0058ee50();
    passed &= verify_synch_veh_00593220();
    passed &= verify_synch_base_00593250();
    passed &= verify_synch_energy_00593280();
    passed &= verify_synch_researching_005932b0();
    passed &= verify_synch_leader_005932e0();
    passed &= verify_synch_ai_00593310();
    passed &= verify_synch_research_00593340();
    passed &= verify_synch_alloc_00593390();
    passed &= verify_synch_soc_005933c0();
    passed &= verify_synch_proto_005933f0();
    passed &= verify_synch_obs_00593420();
    passed &= verify_synch_diplo_00593450();
    passed &= verify_synch_template_005934b0();
    passed &= verify_synch_radius_005934e0();
    passed &= verify_do_sound_005fd2b0();
    std::printf("generated signature oracles: %d function(s)\n", 36);
    return passed;
}
